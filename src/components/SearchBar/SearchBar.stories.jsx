import { SearchBar } from "./SearchBar";

export default {
  title: "SearchBar",
  component: SearchBar,
  tags: ["autodocs"],
};

export const Separate = {
  args: {
    variant: "separate",
  },
};
